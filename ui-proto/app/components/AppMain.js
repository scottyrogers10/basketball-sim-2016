BASE.require([
    "jQuery"
], function () {

    BASE.namespace("app.components");

    app.components.AppMain = function (elem, tags, services) {
        var self = this;
        var $elem = $(elem);

    };
});
